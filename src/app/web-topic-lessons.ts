import { Lesson } from './lesson.model';

const lesson = (id: number, track: Lesson['track'], level: string, title: string, summary: string, concept: string, example: string, quiz: Lesson['quiz'], accent: Lesson['accent'] = 'blue'): Lesson => ({
  id, track, level, title, summary, duration: '28 min', tag: track ?? 'Web', accent, concept, analogy: `Think of ${title.toLowerCase()} as a tool in a well-organized workshop: it has one job, clear inputs, and a predictable result.`, example, takeaway: `Understand the rule, practice it in a small example, then test the edge cases for ${title.toLowerCase()}.`, quiz
});

export const webTopicLessons: Lesson[] = [
  lesson(105, 'HTML', 'Build the basics', 'Headings, links and navigation', 'Create content people can scan and move through.', 'Headings form an outline of the page. Links move between locations or resources, while buttons perform actions. A useful heading order and descriptive link text help sighted readers, screen readers, search engines, and keyboard users.', `<nav aria-label="Primary">\n  <a href="/courses">Courses</a>\n  <a href="/about">About us</a>\n</nav>\n\n<main>\n  <h1>Angular Atlas</h1>\n  <h2>Beginner courses</h2>\n  <a href="/courses/signals">Learn signals</a>\n</main>`, { question: 'Which element navigates to another URL?', options: ['a with href', 'button only', 'span'], answer: 0 }, 'gold'),
  lesson(106, 'HTML', 'Build the basics', 'Lists, tables and data', 'Present repeated items and structured data with the right elements.', 'Use ul or ol for lists, dl for term definitions, and table for two-dimensional data with headers. Tables need a caption and header cells so assistive technology can associate values with their meaning. Do not use tables for page layout.', `<table>\n  <caption>Course progress</caption>\n  <thead><tr><th scope="col">Course</th><th scope="col">Status</th></tr></thead>\n  <tbody><tr><td>Signals</td><td>Started</td></tr></tbody>\n</table>`, { question: 'What should a data table use for column headings?', options: ['th with scope', 'td with bold CSS', 'div'], answer: 0 }, 'coral'),
  lesson(107, 'HTML', 'Build real apps', 'Accessibility and ARIA', 'Make pages usable with keyboards and assistive technology.', 'Start with semantic HTML, then add ARIA only when native semantics cannot express the widget. Every interactive control needs an accessible name, keyboard operation, visible focus, and a state that can be understood. Test with the keyboard, zoom, and a screen reader.', `<button type="button" aria-expanded="false" aria-controls="menu">\n  Courses\n</button>\n<nav id="menu" hidden>\n  <a href="/courses">All courses</a>\n</nav>`, { question: 'What should come before adding ARIA?', options: ['Semantic HTML', 'A div for every control', 'Mouse-only events'], answer: 0 }, 'mint'),
  lesson(108, 'HTML', 'Build real apps', 'Metadata and SEO', 'Give browsers, search engines and social previews useful information.', 'The head contains document metadata rather than visible page content. A unique title, description, canonical URL, language, viewport, and meaningful structured headings help discovery and sharing. SEO cannot repair unclear content structure, so start with useful page content.', `<head>\n  <title>Signals course | Angular Atlas</title>\n  <meta name="description" content="Learn Angular signals with examples.">\n  <link rel="canonical" href="https://example.com/signals">\n  <meta property="og:title" content="Signals course">\n</head>`, { question: 'Where does the page title belong?', options: ['Inside head in a title element', 'Inside a footer only', 'In a CSS variable'], answer: 0 }, 'gold'),
  lesson(109, 'HTML', 'Go further', 'Web components and custom elements', 'Understand reusable browser-native component boundaries.', 'Custom elements let you define reusable HTML tags with a lifecycle. Shadow DOM can isolate markup and styles. Use this platform when framework-independent reuse matters, and understand the tradeoffs around styling, events, forms, and server rendering before choosing it.', `class CourseBadge extends HTMLElement {\n  connectedCallback() {\n    this.textContent = this.getAttribute('level') ?? 'course';\n  }\n}\ncustomElements.define('course-badge', CourseBadge);`, { question: 'What API registers a custom HTML element?', options: ['customElements.define', 'document.registerStyle', 'html.createTag'], answer: 0 }, 'violet'),
  lesson(110, 'HTML', 'Go further', 'Progressive enhancement', 'Build a useful baseline before adding JavaScript behavior.', 'Progressive enhancement starts with meaningful HTML, improves presentation with CSS, and adds JavaScript where it provides extra capability. Forms should still communicate intent, links should still navigate, and important content should not depend on a failed script.', `<form action="/search" method="get">\n  <label for="q">Search</label>\n  <input id="q" name="q">\n  <button>Search</button>\n</form>\n<!-- JavaScript can enhance this with live results. -->`, { question: 'What should work before JavaScript enhancement?', options: ['The core HTML task', 'Only the loading spinner', 'Nothing'], answer: 0 }, 'mint'),
  lesson(205, 'CSS', 'Start here', 'Typography and color', 'Make text readable and visual hierarchy intentional.', 'Typography controls reading comfort through font family, size, line height, weight, and measure. Color should communicate hierarchy and state while meeting contrast requirements. Use design tokens with custom properties so themes change consistently.', `:root {\n  --text: #27332f;\n  --muted: #687169;\n  --accent: #ef684f;\n}\n\nbody {\n  color: var(--text);\n  font: 1rem/1.6 Manrope, sans-serif;\n}`, { question: 'Why use CSS custom properties for colors?', options: ['They centralize reusable design values', 'They make HTML semantic', 'They replace media queries'], answer: 0 }, 'gold'),
  lesson(206, 'CSS', 'Build the basics', 'Positioning and stacking', 'Place overlays, sticky navigation, and anchored UI predictably.', 'Normal flow is the default and should do most of the work. Relative positioning creates a containing block, absolute positioning removes an item from flow, fixed attaches to the viewport, and sticky switches behavior while scrolling. z-index only works within stacking contexts, so understand which context owns the element.', `.toolbar { position: sticky; top: 0; z-index: 10; }\n\n.card { position: relative; }\n.badge {\n  position: absolute;\n  top: 12px; right: 12px;\n}`, { question: 'What does position: sticky do?', options: ['Behaves in flow until a scroll threshold, then sticks', 'Always attaches to the viewport', 'Deletes the element from layout'], answer: 0 }, 'coral'),
  lesson(207, 'CSS', 'Build the basics', 'Flexbox deeply', 'Align toolbars, cards, and one-dimensional layouts.', 'Flexbox works along a main axis and a cross axis. flex-direction chooses the main axis, justify-content distributes along it, align-items aligns across it, and flex properties control growth, shrink, and basis. Use gap rather than margin hacks for relationships between children.', `.toolbar {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 1rem;\n}\n\n.toolbar__actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: .5rem;\n}`, { question: 'Which property aligns flex items on the cross axis?', options: ['align-items', 'justify-content always', 'grid-template'], answer: 0 }, 'blue'),
  lesson(208, 'CSS', 'Build real apps', 'Grid and intrinsic layout', 'Build resilient two-dimensional layouts without fixed hacks.', 'CSS Grid defines tracks and areas while the browser still participates in sizing. minmax, min-content, max-content, auto-fit, and auto-fill let content influence the layout. min-width: 0 on grid children prevents long content from forcing a track wider than its container.', `.dashboard {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(min(100%, 260px), 1fr));\n  gap: 1rem;\n}\n\n.dashboard > * { min-width: 0; }`, { question: 'Why can min-width: 0 be needed in a grid child?', options: ['To let long content shrink within its track', 'To make the child invisible', 'To disable grid'], answer: 0 }, 'mint'),
  lesson(209, 'CSS', 'Go further', 'Transitions, animations and keyframes', 'Communicate state changes without harming usability.', 'Transitions interpolate between states. Keyframes describe a timeline. Animate transform and opacity when possible because they are usually cheaper than layout properties. Respect reduced-motion preferences and never make an animation the only way a state is understood.', `@keyframes reveal {\n  from { opacity: 0; transform: translateY(8px); }\n  to { opacity: 1; transform: translateY(0); }\n}\n\n.panel { animation: reveal 180ms ease-out both; }`, { question: 'Which properties are commonly efficient to animate?', options: ['transform and opacity', 'width and top always', 'font-family and content'], answer: 0 }, 'violet'),
  lesson(210, 'CSS', 'Go further', 'CSS architecture and layers', 'Organize styles so features remain changeable as the app grows.', 'Choose a naming convention, keep component styles scoped where possible, and use cascade layers to define precedence intentionally. Separate tokens, base rules, components, utilities, and overrides. Avoid selectors whose specificity grows every time a feature changes.', `@layer reset, base, components, utilities;\n\n@layer components {\n  .course-card { padding: 1rem; }\n}\n\n@layer utilities { .u-hidden { display: none; } }`, { question: 'What does a CSS architecture protect against?', options: ['Unclear ownership and specificity conflicts', 'All network failures', 'TypeScript errors'], answer: 0 }, 'coral'),
  lesson(305, 'JavaScript', 'Start here', 'Arrays and data transformation', 'Use map, filter, reduce and sort to express collection work.', 'Array methods make transformations visible. map creates a value for every item, filter keeps matching items, reduce accumulates one result, and sort mutates unless you copy first. Prefer chains that read like the business operation and split complex pipelines into named steps.', `const visible = courses\n  .filter(course => course.published)\n  .map(course => ({ id: course.id, label: course.title }))\n  .toSorted((a, b) => a.label.localeCompare(b.label));`, { question: 'Which method creates one output for every input item?', options: ['map', 'filter', 'reduce'], answer: 0 }, 'gold'),
  lesson(306, 'JavaScript', 'Build the basics', 'Scope, closures and this', 'Understand how functions remember data and how context is chosen.', 'Lexical scope determines which variables a function can access. A closure preserves access to variables from its creation context. this is determined by how a function is called, while arrow functions capture the surrounding this. These rules explain callbacks, factories, and many framework APIs.', `function makeCounter() {\n  let value = 0;\n  return () => ++value;\n}\n\nconst next = makeCounter();\nnext(); // 1\nnext(); // 2`, { question: 'What does a closure preserve?', options: ['Access to variables from its outer scope', 'A copy of the DOM', 'A CSS cascade'], answer: 0 }, 'coral'),
  lesson(307, 'JavaScript', 'Build the basics', 'Modules and package design', 'Split code into explicit, reusable imports and exports.', 'ES modules have their own scope. Named exports are useful when a file provides several related values; default exports can represent one primary value. Keep dependency direction intentional, avoid circular imports, and expose a small public API from feature folders.', `// format.ts\nexport function formatCourse(title: string) {\n  return title.trim();\n}\n\n// page.ts\nimport { formatCourse } from './format.js';\nconst label = formatCourse(' Signals ');`, { question: 'What do ES modules provide?', options: ['File scope and explicit imports/exports', 'Automatic database access', 'CSS inheritance'], answer: 0 }, 'blue'),
  lesson(308, 'JavaScript', 'Build real apps', 'The event loop and concurrency', 'Understand tasks, microtasks, rendering, and why timing surprises happen.', 'JavaScript runs one piece of synchronous code at a time, while the host environment schedules tasks. Promise callbacks run as microtasks before the next task, and rendering can happen between turns. Avoid blocking the main thread with large synchronous work; split or move expensive work when needed.', `console.log('A');\nsetTimeout(() => console.log('B'));\nqueueMicrotask(() => console.log('C'));\nconsole.log('D');\n// A, D, C, B`, { question: 'Which runs before a timer callback in this example?', options: ['The queued microtask', 'The timer always', 'A CSS transition callback only'], answer: 0 }, 'mint'),
  lesson(309, 'JavaScript', 'Go further', 'Errors, debugging and defensive code', 'Make failures observable, meaningful, and recoverable.', 'Throw errors at the boundary where an invariant fails, add context when rethrowing, and distinguish expected user errors from programmer defects. Use browser DevTools, source maps, breakpoints, network inspection, and structured logging instead of guessing. Never silently catch an error without a recovery or reporting plan.', `async function load() {\n  try {\n    return await api.getCourses();\n  } catch (cause) {\n    console.error('Course request failed', { cause });\n    throw new Error('Courses unavailable', { cause });\n  }\n}`, { question: 'Why is an empty catch block dangerous?', options: ['It hides failures without recovery or reporting', 'It makes code asynchronous', 'It improves security automatically'], answer: 0 }, 'coral'),
  lesson(310, 'JavaScript', 'Go further', 'Storage, security and Web APIs', 'Use browser capabilities without treating the browser as trusted storage.', 'localStorage is persistent string storage, sessionStorage is scoped to a tab session, and IndexedDB supports larger structured data. Browser storage is readable by code running in the origin, so never store secrets there. Validate URL, HTML, and server data before using them.', `const saved = localStorage.getItem('theme');\nlocalStorage.setItem('theme', 'dark');\n\nconst controller = new AbortController();\nfetch('/api/courses', { signal: controller.signal });\ncontroller.abort();`, { question: 'Should private tokens be stored in localStorage?', options: ['No, browser JavaScript can access it', 'Yes, it encrypts all values', 'Only when CSS is disabled'], answer: 0 }, 'violet'),
  lesson(405, 'TypeScript', 'Start here', 'Narrowing and type guards', 'Turn unknown values into safe, usable types.', 'Narrowing uses runtime checks to reduce a union to a more specific type. typeof handles primitives, in checks properties, instanceof checks classes, and custom type predicates tell TypeScript about a reusable proof. Use unknown for external values instead of pretending they are safe.', `function isCourse(value: unknown): value is Course {\n  return typeof value === 'object' && value !== null\n    && 'id' in value && 'title' in value;\n}\n\nif (isCourse(payload)) console.log(payload.title);`, { question: 'What does a type predicate such as value is Course do?', options: ['Tells TypeScript the value is narrowed after a check', 'Validates every API response automatically', 'Creates a runtime class'], answer: 0 }, 'gold'),
  lesson(406, 'TypeScript', 'Build the basics', 'Enums, literal types and discriminated unions', 'Represent finite states without allowing random strings.', 'Literal unions are often simpler than enums for application state. A discriminant property lets a switch or if statement narrow the rest of an object. Exhaustive checks can make the compiler alert you when a new state is added but not handled.', `type RequestState =\n  | { status: 'idle' }\n  | { status: 'loading' }\n  | { status: 'success'; data: Course[] }\n  | { status: 'error'; message: string };\n\nfunction render(state: RequestState) {\n  switch (state.status) {\n    case 'success': return state.data.length;\n    case 'error': return state.message;\n    default: return state.status;\n  }\n}`, { question: 'What property narrows a discriminated union?', options: ['A shared literal discriminant such as status', 'Any CSS class', 'The file name'], answer: 0 }, 'coral'),
  lesson(407, 'TypeScript', 'Build the basics', 'Generics in real applications', 'Keep reusable functions, stores, and API clients type-safe.', 'A generic type parameter preserves a relationship between values. Constraints limit what types are allowed. Generic interfaces and classes are useful for repositories, paginated responses, table components, and state containers. Avoid generic code when a concrete type would be clearer.', `interface Page<T> {\n  items: T[];\n  total: number;\n}\n\nfunction last<T>(items: readonly T[]): T | undefined {\n  return items.at(-1);\n}\n\nconst page: Page<Course> = { items: [], total: 0 };`, { question: 'Why use a generic Page<T> type?', options: ['The page keeps the item type wherever it is used', 'It converts all values to strings', 'It removes runtime data'], answer: 0 }, 'blue'),
  lesson(408, 'TypeScript', 'Build real apps', 'Mapped and conditional types', 'Create types that transform other types according to rules.', 'Mapped types iterate over keys of another type. Conditional types choose a result based on assignability. They are powerful for forms, permissions, API states, and libraries, but can become unreadable. Name complex types and prefer a simpler explicit type when the transformation is not obvious.', `type ReadonlyFields<T> = {\n  readonly [K in keyof T]: T[K];\n};\n\ntype ApiResult<T> = T extends Error\n  ? { ok: false; error: T }\n  : { ok: true; data: T };`, { question: 'What does a mapped type do?', options: ['Transforms properties across another type', 'Maps DOM pixels to CSS', 'Runs a loop at runtime'], answer: 0 }, 'mint'),
  lesson(409, 'TypeScript', 'Go further', 'Declaration files and library types', 'Understand how TypeScript describes JavaScript libraries.', 'Declaration files ending in .d.ts describe types without emitting JavaScript. Libraries publish types so consumers get autocomplete and checks. You can declare a global, module, or missing package carefully, but inaccurate declarations are worse than a visible unknown boundary.', `declare module '*.md' {\n  const content: string;\n  export default content;\n}\n\ndeclare global {\n  interface Window { analytics?: Analytics; }\n}`, { question: 'What is the purpose of a .d.ts file?', options: ['Describe types for code that does not emit JavaScript', 'Store CSS variables', 'Run tests'], answer: 0 }, 'violet'),
  lesson(410, 'TypeScript', 'Go further', 'Compiler configuration and project references', 'Make a large TypeScript codebase consistent and fast to compile.', 'tsconfig controls strictness, module and target output, path aliases, source maps, declaration output, and included files. Project references can split a large repository into buildable projects. Keep application, test, and library settings intentional instead of weakening strictness globally to silence one problem.', `{\n  "compilerOptions": {\n    "strict": true,\n    "noUncheckedIndexedAccess": true,\n    "noUnusedLocals": true,\n    "moduleResolution": "bundler"\n  }\n}`, { question: 'Why should strictness usually remain enabled?', options: ['It exposes unsafe assumptions at compile time', 'It prevents all runtime errors', 'It is only for CSS'], answer: 0 }, 'gold')
];
