import { Lesson } from './lesson.model';

export const additionalLessons: Lesson[] = [
  { id: 13, level: 'Start here', title: 'Project setup & CLI', summary: 'Understand the files, commands, and choices created by Angular CLI.', duration: '20 min', tag: 'Foundations', accent: 'blue', concept: 'The CLI is your project assistant. ng new creates an application, angular.json describes build targets, package.json records scripts and dependencies, src/main.ts bootstraps the app, and src/app contains the feature code. Learn the difference between development serve, production build, test, and generate commands before adding libraries. The CLI keeps configuration consistent and makes common changes repeatable.', analogy: 'The CLI is the workshop where your project receives its tools, labels, and repeatable instructions before you start building furniture.', example: `ng new study-app --routing --style=css\ncd study-app\nng serve\nng generate component lessons/lesson-card\nng build`, takeaway: 'Know what every generated file does before you delete or customize it.', quiz: { question: 'Which command starts the local Angular development server?', options: ['ng serve', 'ng start-app', 'angular run'], answer: 0 } },
  { id: 14, level: 'Start here', title: 'TypeScript you need', summary: 'Learn the TypeScript ideas that make Angular code readable and safe.', duration: '30 min', tag: 'Foundations', accent: 'gold', concept: 'Angular is written in TypeScript, so types are part of how you communicate intent. Learn primitives, arrays, object types, interfaces, unions, optional properties, generics, classes, access modifiers, and async return types. Strict mode makes the compiler ask useful questions, such as whether a value can be null. A type does not validate runtime data from an API; it documents the shape your code expects.', analogy: 'Types are labels on storage boxes. They do not inspect what arrives at your door, but they stop you from putting a number into the box labeled email.', example: `interface Course {\n  id: number;\n  title: string;\n  level: 'beginner' | 'advanced';\n}\n\nfunction first<T>(items: T[]): T | undefined {\n  return items[0];\n}\n\nconst course: Course = { id: 1, title: 'Signals', level: 'advanced' };`, takeaway: 'Use types to make invalid states harder to represent, not to decorate every line.', quiz: { question: 'What does a union type describe?', options: ['One of several allowed types or values', 'A CSS class', 'A database join'], answer: 0 } },
  { id: 15, level: 'Build the basics', title: 'Inputs & outputs', summary: 'Make components communicate through clear public APIs.', duration: '24 min', tag: 'Components', accent: 'coral', concept: 'A child component should not reach into its parent. It accepts data through input signals and sends events through output emitters. This creates a one-way data flow: the parent owns state, the child displays it and reports user intent. Required inputs document what a component cannot work without, and aliases can keep public names friendly.', analogy: 'A component is a vending machine. You put ingredients in through inputs, press a button, and receive an output; you never open the machine to rearrange its wires.', example: `@Component({\n  selector: 'app-course-card',\n  template: \`<button (click)="selected.emit(course().id)">\n    {{ course().title }}\n  </button>\`\n})\nexport class CourseCard {\n  course = input.required<Course>();\n  selected = output<number>();\n}`, takeaway: 'Keep ownership in the parent and make child APIs small and explicit.', quiz: { question: 'What should a child use to send an event to its parent?', options: ['An output', 'A private field', 'A route guard'], answer: 0 } },
  { id: 16, level: 'Build the basics', title: 'Lifecycle & rendering', summary: 'Know when Angular creates, updates, and destroys a component.', duration: '22 min', tag: 'Components', accent: 'mint', concept: 'Lifecycle hooks are moments in a component’s lifetime. Use constructor or field initialization for simple setup, ngOnInit for initialization that depends on inputs, ngOnChanges when an input change needs a response, afterNextRender for browser rendering work, and ngOnDestroy to release resources. Prefer signals and computed values for normal data flow instead of using hooks as a hidden event system.', analogy: 'A component has a morning, working day, and closing time. Lifecycle hooks are notices at those moments, not places to put every task in the building.', example: `export class Chart implements OnInit, OnDestroy {\n  private stop = inject(DestroyRef);\n  data = input.required<Point[]>();\n\n  ngOnInit() {\n    console.log('Initial data is ready');\n  }\n\n  ngOnDestroy() {\n    this.chart?.destroy();\n  }\n}`, takeaway: 'Use the hook that matches the timing you actually need.', quiz: { question: 'Where should cleanup for a component-owned resource happen?', options: ['ngOnDestroy', 'ngAfterEveryRender only', 'The HTML template'], answer: 0 } },
  { id: 17, level: 'Build the basics', title: 'Pipes & formatting', summary: 'Transform display values without mutating the source data.', duration: '18 min', tag: 'Templates', accent: 'violet', concept: 'Pipes are template functions for presentation: dates, currency, numbers, JSON, and custom formatting. Built-in pipes are pure by default, so Angular can reuse results when the input reference is unchanged. Keep business decisions out of pipes. If formatting is expensive or reused outside templates, put it in a plain function or service instead.', analogy: 'A pipe is a display filter on a camera. It changes how the picture is shown, not what is stored in the scene.', example: `@Component({\n  imports: [CurrencyPipe, DatePipe],\n  template: \`\n    <p>{{ course.price | currency:'USD' }}</p>\n    <time>{{ course.updatedAt | date:'mediumDate' }}</time>\n  \`\n})\nexport class CourseSummary {\n  course = input.required<Course>();\n}`, takeaway: 'Format at the edge of the UI and keep your domain values meaningful.', quiz: { question: 'What is the main job of a pipe?', options: ['Format a value for display', 'Fetch a server resource', 'Create a component'], answer: 0 } },
  { id: 18, level: 'Build the basics', title: 'Attribute & structural directives', summary: 'Reuse small pieces of DOM behavior with directives.', duration: '25 min', tag: 'Templates', accent: 'blue', concept: 'An attribute directive changes the behavior or appearance of an existing element. A structural directive changes which DOM exists, although modern Angular control flow handles many common cases. Use directives for focused reusable behavior such as keyboard shortcuts, focus management, or a selected visual state. Avoid directives that secretly own large application workflows.', analogy: 'A directive is an attachment for a tool: it gives an existing element one extra capability without replacing the tool itself.', example: `@Directive({ selector: '[appHighlight]' })\nexport class HighlightDirective {\n  private el = inject(ElementRef<HTMLElement>);\n\n  @HostListener('mouseenter') enter() {\n    this.el.nativeElement.style.outline = '2px solid coral';\n  }\n  @HostListener('mouseleave') leave() {\n    this.el.nativeElement.style.outline = '';\n  }\n}`, takeaway: 'A directive should add one behavior that remains easy to name and test.', quiz: { question: 'What does an attribute directive usually change?', options: ['An element’s behavior or appearance', 'The application database', 'The URL server'], answer: 0 } },
  { id: 19, level: 'Build real apps', title: 'Route params, guards & resolvers', summary: 'Load the right data and protect navigation deliberately.', duration: '30 min', tag: 'Routing', accent: 'gold', concept: 'Route parameters identify a resource such as /courses/42. Query parameters describe optional view state such as ?tab=reviews. Guards decide whether navigation may proceed; they should stay focused on access or unsaved work. Resolvers load critical data before activation, but use them carefully because a slow resolver delays the whole screen. Often a component-level loading state feels better.', analogy: 'A route is a hotel reservation: the path identifies the room, query parameters describe preferences, guards check the key, and a resolver prepares what must be waiting inside.', example: `export const routes: Routes = [{\n  path: 'courses/:id',\n  canActivate: [authGuard],\n  resolve: { course: courseResolver },\n  loadComponent: () => import('./course-page')\n    .then(m => m.CoursePage)\n}];\n\nexport const authGuard: CanActivateFn = () =>\n  inject(AuthService).isSignedIn() || inject(Router).parseUrl('/login');`, takeaway: 'Use routing tools for navigation concerns, not as a replacement for application state.', quiz: { question: 'What is a route parameter best used for?', options: ['Identifying a resource', 'Styling a button', 'Declaring a class'], answer: 0 } },
  { id: 20, level: 'Build real apps', title: 'Dynamic and nested forms', summary: 'Model arrays, conditional fields, and complex user input.', duration: '32 min', tag: 'Forms', accent: 'coral', concept: 'FormArray represents a variable number of controls, such as skills or line items. Nested FormGroup objects represent related fields. Build the form model from the user’s task, then make the template a projection of that model. Dynamic forms need accessible labels, keyboard support, and clear rules for adding, removing, and reordering items.', analogy: 'A FormArray is a notebook with detachable rows. The user can add a page, remove one, or edit a page while the notebook still knows the whole structure.', example: `skills = new FormArray<FormControl<string>>([]);\nprofile = new FormGroup({\n  name: new FormControl('', { nonNullable: true }),\n  skills: this.skills\n});\n\naddSkill() {\n  this.skills.push(new FormControl('', { nonNullable: true }));\n}\nremoveSkill(index: number) { this.skills.removeAt(index); }`, takeaway: 'The form model should describe the user’s task, not mirror the visual layout blindly.', quiz: { question: 'Which control models a variable-length list of fields?', options: ['FormArray', 'FormLabel', 'RouteArray'], answer: 0 } },
  { id: 21, level: 'Build real apps', title: 'Http interceptors', summary: 'Handle auth headers, errors, retries, and loading in one boundary.', duration: '26 min', tag: 'Network', accent: 'mint', concept: 'An interceptor observes requests and responses globally. It can add an authorization header, record timing, map known errors, or refresh a token. Keep interceptors narrow: a global policy belongs there, while a feature-specific decision belongs in its service. Clone immutable HttpRequest objects rather than mutating them.', analogy: 'An interceptor is the mailroom. Every package passes through it for a stamp or tracking number, but the mailroom should not rewrite the contents of every box.', example: `export const authInterceptor: HttpInterceptorFn = (request, next) => {\n  const token = inject(AuthService).token();\n  const secured = token ? request.clone({\n    setHeaders: { Authorization: \`Bearer \${token}\` }\n  }) : request;\n  return next(secured);\n};`, takeaway: 'Centralize repeated transport policy, but keep business behavior near its feature.', quiz: { question: 'How should an HttpRequest be changed in an interceptor?', options: ['Clone it', 'Mutate it directly', 'Rewrite index.html'], answer: 0 } },
  { id: 22, level: 'Build real apps', title: 'State architecture', summary: 'Choose local, shared, server, and URL state without overengineering.', duration: '34 min', tag: 'Architecture', accent: 'violet', concept: 'Not every value needs a global store. Keep ephemeral UI state local. Put shared client state in a service with signals when a few features need it. Keep server state close to its loading and cache policy. Put shareable, restorable view state in the URL. A state model should make ownership, events, and derived values obvious.', analogy: 'State is luggage. Keep a wallet in your pocket, shared supplies in a cupboard, and a destination on the map. Do not build a warehouse for every item.', example: `@Injectable({ providedIn: 'root' })\nexport class CartStore {\n  private items = signal<CartItem[]>([]);\n  readonly count = computed(() =>\n    this.items().reduce((total, item) => total + item.quantity, 0));\n  readonly snapshot = this.items.asReadonly();\n\n  add(item: CartItem) {\n    this.items.update(items => [...items, item]);\n  }\n}`, takeaway: 'Choose state scope by ownership and lifetime, not by fashion.', quiz: { question: 'Where does a shareable filter usually belong?', options: ['The URL query parameters', 'A random component field', 'CSS'], answer: 0 } },
  { id: 23, level: 'Go further', title: 'Advanced RxJS patterns', summary: 'Handle cancellation, concurrency, errors, and cleanup with confidence.', duration: '38 min', tag: 'Advanced', accent: 'blue', concept: 'Higher-order Observables produce streams from values. switchMap cancels stale work, concatMap queues work, exhaustMap ignores new work while busy, and mergeMap runs concurrently. catchError changes how a stream fails, retryWhen can recover from temporary failures, and finalize runs at the end. Understand the concurrency contract before choosing an operator.', analogy: 'These operators are dispatch rules for a busy kitchen: replace the old order, queue it, ignore duplicates, or cook several orders at once.', example: `saveClicks$.pipe(\n  exhaustMap(() => this.api.save(this.form.getRawValue()).pipe(\n    retry({ count: 2, delay: 500 }),\n    catchError(error => {\n      this.message.set('Save failed');\n      return EMPTY;\n    }),\n    finalize(() => this.saving.set(false))\n  ))\n);`, takeaway: 'Pick an RxJS operator by the user action it must protect.', quiz: { question: 'Which operator ignores new clicks while work is active?', options: ['exhaustMap', 'switchMap', 'map'], answer: 0 } },
  { id: 24, level: 'Go further', title: 'Accessibility in Angular', summary: 'Make components usable with keyboards, screen readers, and zoom.', duration: '30 min', tag: 'Quality', accent: 'gold', concept: 'Accessibility is part of component correctness. Use semantic HTML before ARIA, keep focus visible, label controls, announce async errors, preserve heading order, and ensure keyboard interaction. Angular CDK provides utilities for focus management, overlays, and accessible patterns. Test with keyboard-only navigation and automated tools, but also understand the human experience.', analogy: 'Accessibility is a door that works for more people. A beautiful entrance with no handle is still a broken entrance.', example: `<button type="button"\n  [attr.aria-expanded]="isOpen()"\n  aria-controls="course-menu"\n  (click)="isOpen.update(open => !open)">\n  Courses\n</button>\n<nav id="course-menu" [hidden]="!isOpen()">\n  <a routerLink="/courses">All courses</a>\n</nav>`, takeaway: 'Use native semantics first, then add ARIA only when it communicates missing meaning.', quiz: { question: 'What should you prefer before adding ARIA?', options: ['Semantic HTML', 'A div for every element', 'Mouse-only events'], answer: 0 } },
  { id: 25, level: 'Go further', title: 'Angular CDK & Material thinking', summary: 'Use battle-tested primitives for overlays, tables, drag-drop, and more.', duration: '28 min', tag: 'Advanced', accent: 'coral', concept: 'The Angular CDK provides behavior without forcing a visual design: overlay positioning, dialog infrastructure, a11y utilities, drag and drop, virtual scrolling, clipboard, and layout tools. Angular Material builds a visual component library on those primitives. Learn to customize tokens and preserve the library’s interaction guarantees instead of rebuilding complex behavior from scratch.', analogy: 'The CDK is a box of reliable mechanical parts. You can build your own vehicle body, but you do not need to reinvent the brakes.', example: `import { CdkDrag, CdkDropList, moveItemInArray } from '@angular/cdk/drag-drop';\n\nreorder(event: CdkDragDrop<Course[]>) {\n  moveItemInArray(this.courses, event.previousIndex, event.currentIndex);\n  this.courseService.saveOrder(this.courses).subscribe();\n}`, takeaway: 'Reuse proven interaction primitives where keyboard, focus, and geometry are difficult.', quiz: { question: 'What is the main purpose of Angular CDK?', options: ['Reusable interaction primitives', 'A database engine', 'A CSS reset only'], answer: 0 } },
  { id: 26, level: 'Go further', title: 'SSR, hydration & rendering', summary: 'Choose client, server, or hybrid rendering for the right user experience.', duration: '36 min', tag: 'Advanced', accent: 'mint', concept: 'Client-side rendering builds the page in the browser. Server-side rendering sends useful HTML from a server, which can improve first content and search indexing. Hydration attaches Angular behavior to server-rendered markup instead of rebuilding it. Browser-only APIs must be isolated, and data requests need a transfer strategy so the browser does not fetch the same data again.', analogy: 'CSR is assembling furniture at home, SSR is receiving it assembled, and hydration is quietly attaching the controls without rebuilding the furniture.', example: `// server-safe component code\n@Component({\n  template: \`<h1>{{ title() }}</h1>\`\n})\nexport class CoursePage {\n  private platformId = inject(PLATFORM_ID);\n  title = signal('Angular');\n\n  openPrintDialog() {\n    if (isPlatformBrowser(this.platformId)) window.print();\n  }\n}`, takeaway: 'Rendering strategy is a product decision about speed, SEO, and runtime constraints.', quiz: { question: 'What does hydration do?', options: ['Adds behavior to server HTML', 'Deletes all HTML', 'Compresses TypeScript'], answer: 0 } },
  { id: 27, level: 'Go further', title: 'Security & trusted templates', summary: 'Protect user data and understand Angular’s built-in defenses.', duration: '32 min', tag: 'Production', accent: 'coral', concept: 'Angular treats template values as untrusted and escapes interpolation to help prevent XSS. Avoid bypassSecurityTrust APIs unless you understand and control the source. Protect the server too: client guards are not authorization, secrets never belong in browser bundles, and APIs need server-side validation, authentication, authorization, and CSRF strategy where applicable.', analogy: 'The browser is a public lobby. Angular can keep the display case safe, but the security guard and locked archive must still exist on the server.', example: `// Safe: Angular escapes this value in interpolation\n<p>{{ userComment }}</p>\n\n// Dangerous unless the source is fully controlled\n<div [innerHTML]="trustedHtml"></div>\n\n// Server must still authorize the action\nthis.http.delete(\`/api/users/\${userId}\`);`, takeaway: 'Treat every browser value as untrusted and enforce permissions on the server.', quiz: { question: 'Where must authorization be enforced?', options: ['On the server', 'Only in a route guard', 'Only with CSS'], answer: 0 } },
  { id: 28, level: 'Go further', title: 'Unit, component & E2E testing', summary: 'Build a test pyramid that catches real regressions without becoming brittle.', duration: '40 min', tag: 'Production', accent: 'gold', concept: 'Unit tests cover pure functions quickly. Component tests render a component and interact with visible behavior. HttpTestingController verifies network contracts without a real server. End-to-end tests validate a few complete journeys. Tests should describe behavior and remain stable when internal implementation changes.', analogy: 'A test pyramid is a building: many small strong bricks, fewer room-sized checks, and a small roof of full building tours.', example: `it('shows a validation message', () => {\n  const fixture = TestBed.createComponent(ProfileForm);\n  fixture.detectChanges();\n  const email = fixture.nativeElement.querySelector('#email');\n  email.dispatchEvent(new Event('blur'));\n  fixture.detectChanges();\n  expect(fixture.nativeElement.textContent)\n    .toContain('Email is required');\n});`, takeaway: 'Test what a user can observe and keep the test’s setup close to its behavior.', quiz: { question: 'What makes a UI test less brittle?', options: ['Testing visible behavior', 'Testing private fields', 'Depending on timing'], answer: 0 } },
  { id: 29, level: 'Go further', title: 'Deployment & environments', summary: 'Move an Angular app from local development to a reliable release.', duration: '28 min', tag: 'Production', accent: 'violet', concept: 'A production build optimizes, bundles, and replaces environment configuration. Environment files are not secret storage because anything shipped to the browser is public. Configure the API at deployment time when possible, serve the SPA fallback for deep links, enable compression and caching, and monitor errors after release.', analogy: 'Deployment is opening a shop: compile the stock, put up the right address, make sure every hallway route works, and keep a log of problems customers report.', example: `ng build --configuration production\n\n// web server must route unknown app paths to index.html\nlocation / {\n  try_files $uri $uri/ /index.html;\n}\n\n// never put private keys in environment.ts`, takeaway: 'A release is successful only when users can load, navigate, and recover from failure.', quiz: { question: 'Where should a private API key live?', options: ['On the server', 'In environment.ts shipped to the browser', 'In a template'], answer: 0 } },
  { id: 30, level: 'Go further', title: 'Feature architecture & code review', summary: 'Organize a growing Angular codebase so teams can change it safely.', duration: '42 min', tag: 'Architecture', accent: 'blue', concept: 'Organize by feature and user capability rather than by giant global folders. Keep routes, components, services, models, and tests close to the feature they support. Define boundaries, public APIs, naming conventions, and ownership. During review, ask whether data ownership is clear, states are complete, accessibility works, and the change is tested at the right level.', analogy: 'A good codebase is a neighborhood: each house has a purpose, streets connect intentionally, and no one needs to walk through every house to find the post office.', example: `src/app/\n  courses/\n    data-access/\n      course.api.ts\n      course.store.ts\n    ui/\n      course-card/\n      course-list/\n    pages/\n      course-page/\n    courses.routes.ts\n\n// feature routes own their feature boundary\nexport default coursesRoutes;`, takeaway: 'Architecture is a tool for reducing the cost and risk of the next change.', quiz: { question: 'What is a useful feature boundary?', options: ['A user capability with its own public API', 'One folder for every file type in the app', 'A random group of components'], answer: 0 } }
];
